//substr() method //substr() is similar to slice(). The difference is that the second parameter specifies the length of the extracted part.
var str = "securifi embedded systems at madhapur it is one of embedded systems company";
var string2 = str.substr(-30);
console.log(str)
console.log(string2)
//Note: outptu is=>stems at m
